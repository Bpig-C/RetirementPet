# 1.3 节点 B 交付报告（V13-02 / V13-04 / V13-05 / V13-06）

> 日期：2026-09-16 · 分支：`impl/v1.3-agent-workflow`
> 范围：日常工作流节点（工单第 15 节顺序 2）
> 验收方式：执行者自测 + 三名独立验收员（D/E/F）以程序与数据为准的
> 正反例验收；全部返工项已闭环。

## 实际提交（节点 B 主线 + 验收返工）

| 提交 | 内容 |
|---|---|
| `a6fea05` | V13-02：Agent CLI（todo/action/character/media 四域 + media 桥接线） |
| `7b21e36` | V13-04：Markdown 预览与主树优先级标签 |
| `9270707` | V13-06：活动联动开关与可解释状态 |
| `9e9d5c2` | 设置保存失败回滚不残留新键（回归修复） |
| `ba36a00` | V13-05：系统媒体桥（SMTC provider + 合成 provider + 真机验证） |
| `f814dda`/`25a6336` | todo.list quadrant 筛选 + 文档 media 行（验收员 D 返工） |
| `5d38d74` | 预览图片剥除（file/data 安全）+ 720×480 状态列可见（验收员 E 返工） |
| `47c8fd3` | 持久化媒体开关重启崩溃修复 + 快照即时刷新 + 隐藏门（验收员 F 返工） |

## 用户可见结果

- `RetirementPet.exe agent <domain> <verb>`（源码 `python -m retirement_pet
  agent ...`）可查询状态、管理任务树、触发动作、切换角色、控制系统媒体；
  `--json` 机器输出 + 稳定退出码。
- 备注侧页新增“编辑/预览”切换：预览不联网、不读本地文件、不执行脚本、
  不加载任何图片；http/https 链接需确认后经系统浏览器打开。
- 主树状态列显示 `·要`/`·急` 文字标签（非仅颜色），720×480 全列可见。
- 声音页新增“系统媒体联动”（默认关）与活动联动开关；概览页显示
  “近期有输入/空闲/未知”与专注状态（不含任务标题）。
- 开启媒体联动后可在桌宠侧看到当前控制的播放器（app 名 + 播放状态），
  真实网易云 3.1.39 会话实测播放/暂停/切歌全部生效。

## 测试命令与结果（自测）

- `pytest tests/test_agent_cli.py tests/test_agent_protocol.py`：53 项。
- `pytest tests/test_todo_v13_04_ui.py`：16 项（含像素级图片反例）。
- `pytest tests/test_media_bridge.py tests/test_v13_06_activity.py`：30 项
  （含真机 SMTC 全链路：自建会话出现→桥投影→真实暂停/恢复→消失）。
- 节点 B 回归（11 个文件）：211 passed。
- 真机证据：`evidence/v13/20260916-netease-real-session.json`
  （网易云 3.1.39.205426 会话：play/pause/next 全部 accepted，
  状态 4↔5 翻转；证据不含歌曲标题）。

## 独立验收结论与返工闭环

- **验收员 D（CLI）**：8 条标准全 PASS；独立 12 步端到端三方一致探针 +
  50 项反例 0 失败。返工项：todo.list 补 quadrant 筛选（工单原文要求）、
  文档补 media.* 行 —— 已闭环（`f814dda`/`25a6336`）。
- **验收员 E（Markdown/优先级）**：大部分 PASS；两项高严重度：
  1) `file://`/`data:` 图片被 Qt 文档级加载器绕过 loadResource 自动渲染
  （像素级证实）→ 修复：setMarkdown 前剥除全部 markdown 图片语法，
  仅保留 alt 文本，新增像素级反例测试；2) 720×480 下状态列被裁切出
  视口 → 修复：列宽紧凑化 + 树最小宽度硬约束，新增无横滚断言。
  另修复低/未分类任务 tooltip 缺失与矩阵行未填。全部闭环（`5d38d74`）。
- **验收员 F（媒体桥/活动）**：V13-06 全 PASS；V13-05 一项 P0：持久化
  开启媒体联动后二次启动崩溃（enable 调用先于构造）→ 修复并新增
  重启回归测试；另修复本地播放状态显示过期、隐藏门未生效、原生测试
  环境敏感断言、defaults.json 缺键。全部闭环（`47c8fd3`）。

## 未完成项与对下一节点的影响

- 网易云 next/previous 已实测 accepted（证据 JSON）；自建无曲目的
  MediaPlayer 会话对 skip 的拒绝路径由合成 provider 覆盖。
- 三场景 CPU/内存预算测量并入 V13-09 候选证据（使用仓库正式
  `scripts/perf_sample.py` 流程，本节点临时探针方案已废弃）。
- 节点 C（V13-07 内容 → V13-08 发布材料 → V13-09 候选）可从当前
  稳定源码状态开始；版本号仍保持 1.2.0，待节点 C 签收后收敛 1.3.0。

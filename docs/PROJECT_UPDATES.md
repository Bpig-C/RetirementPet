# RetirementPet 项目更新记录

> 2026-09-15 更新
> 本文记录版本变化与用户可见结果
> 相关入口包括[文档中心](README.md)、[当前实施状态](IMPLEMENTATION_STATUS.md)和
> [未来工作](FUTURE_WORK.md)

本文件按时间追加项目变化。它帮助人理解版本，不替代构建 receipt、验收事件或
`acceptance.json`。精确构建事实一经引用就不回写；后续修订以新条目说明。

## 当前版本轴

| 对象 | 当前版本 | 含义 |
|---|---|---|
| Windows 应用 | 1.2.0 | 当前取得 `ALPHA-GO` 的候选构建；Production 尚未认证 |
| 产品与架构规范 | v2.0 | 平台设计基线，不等同于应用版本 |
| PetPack schema | 1.0 | 角色包数据协议 |
| 官方退休猫 Revision | 1.0.1 | 当前官方安全角色；旧 Revision 仍保留 |
| 半写实退休猫 | 0.1.1 | 随附的 `LOCAL_IMPORTED` 静态画风预览 |

## 2026-09-15

### 1.3.0 下一版本规划（尚未实施）

1.2.0 保留为已签收但未公开的内部 Alpha 基线。根据本地“宠物优化”Todo 的开放项，
下一轮统一规划为 1.3.0：增加版本化本地 Agent JSON/CLI、Todo 删除与子树完成语义、
Markdown 备注预览、重要/紧急可见性、Windows 系统媒体会话联动、工作活动说明和
角色序列内容；同时接续 SBOM/许可/LGPL、脱敏公开快照、回滚与真机门禁。范围、
依赖和完成定义见 [1.3.0 版本工作工单](V1_3_WORK_ORDER.md)。本条只记录规划，当前
源码版本和既有候选身份没有因此改变。

### 1.2.0 日常使用增强 Alpha 候选

V12-01 至 V12-09 的工程范围完成集成：修复 PetPack idle 时间轴和 geometry/
脚底锚布局，加入 Todo schema v2、四象限/归档/长备注、作者工具、配置实际生效、
角色 Revision 管理、可信显示证据链和静态视觉降载。1.2.0 候选从 70c5934 的
干净快照构建，绑定门禁为 23 PASS、1 个真实拓扑事件缺失的环境型 SKIP，结论
`ALPHA-GO`；Production、公开二进制许可材料、旧版配套回滚和 Blender 内容仍开放。
精确私有候选身份见 [V12-09 阶段报告](V1_2_V12_09_REPORT.md)。

## 2026-09-11

### 后续审计与交接记录（未发布）

本轮只完成源树、私有运行状态和路线的审计，没有生成新应用构建或发布版本。审计
发现正常待机的 PetPack 序列会因应用快照时间固定为零而停在首帧；现有多屏验收
脚本也只做设备枚举，却把未实际覆盖的混合 DPI 与负坐标场景记为 PASS。两项已经
进入[未来工作](FUTURE_WORK.md) NEXT，旧 1.1.1 证据不回写，新候选必须修复并重新
绑定 receipt 后才能给出结论。

Todo 中已有的“添加四象限方式分辨优先级”“增加完成后自动归档不显示在主清单”
和“增加长篇备注功能，侧边页面”此前是运行时数据库里的用户规划，没有对应产品
路线。它们现已作为 R5 轻量增强正式接入文档链，前置条件是 schema v2 的迁移、备份
和回滚；个人任务标题、数据库及其他运行时数据仍只留在本机，不复制进仓库。

新增 `R1-Blend-01` 作为更拟真退休小猫的创作交接项。Blender 仅用于离线制作，首批
输出 512×512 透明 PNG 的 idle 循环及独立 work/rest 姿态，确认桌面比例后再补生活
动作；项目不因此引入实时 Blender/3D Runtime。正式接收序列前先修 idle 时间轴和
geometry/脚底锚消费，角色包以新 0.2.0 Revision 发布，并保留完整来源与许可记录。

## 2026-09-01

### 半写实猫 0.1.1 公开净化 Revision

0.1.0 的角色 PNG 含一个公开媒体 allowlist 之外的 `caBX` ancillary chunk。没有
覆盖这份旧事实：内部仓库继续冻结原 0.1.0 canonical，公开快照只按完整相对路径
排除该旧包。新 0.1.1 使用仓库内确定性工具移除该 chunk，不重新编码图片；全部
IDAT 与 QImage RGBA8888 解码结果逐字节一致。应用 pin、smoke gate、测试、资产清单
和作者记录同步指向新 Revision。

### 公开源码基线

本轮为公开仓库建立 MIT 代码与文档许可、贡献指南、安全报告入口、PR/Issue 模板、
只读权限的 Windows CI 和机器中立公开快照。`main` 的远端规则要求 PR、测试和维护者
审核；角色内容必须说明作者、来源、许可、AI 使用和公开再分发权。

公开快照从当前干净 HEAD 读取 Git blobs，建立全新的单 root commit，不继承私有
开发历史。它只保留脱敏的 `evidence/README.md`，排除其余 raw evidence、`.release`、
虚拟环境、构建目录和本地数据库，并拒绝个人路径、机器标识、高置信密钥和不安全
归档。私有原始事实继续追加保存，不因公开导出而覆盖或删除。

现有 1.1.1 onedir 没有携带完整第三方许可与 NOTICE，不能作为公开二进制上传。
首次公开基线因此是源码预览；便携版须从公开提交重建，并把实际组件清单、许可文本、
LGPL 对应源码入口和 artifact 摘要纳入新门禁。

### 1.1.1

用户可见变化包括一次性角色选择。手动启动时，用户可以继续使用官方退休猫，也
可以明确确认本地内容边界后启用随附半写实猫。关闭面板或预检失败不会误记选择
完成；开机启动保持静默。

这一版继续保留 1.1.0 的本地角色包导入、角色切换、Todo 树和
`Ctrl+Alt+T` 快捷入口，并修正高 DPI 正文绘制和中文文案后备路径。

当前参考构建的公开摘要如下。完整 source/build/artifact/EXE 身份留在私有追加式
证据域，不复制到公开仓库。

| 字段 | 值 |
|---|---|
| 身份详情 | 私有证据域，不公开内部 commit、tree、build、artifact 或 EXE 摘要 |
| 完整目录 | 259 个文件，121.33 MiB |
| Alpha 结论 | `ALPHA-GO` |
| Production 结论 | `PRODUCTION-NO-GO:PENDING_ENVIRONMENT` |

原始 acceptance 和 receipt 保存在私有追加式证据域，不进入公开仓库或 Release。
公开记录只引用脱敏身份摘要，也不能把这份结果复制成另一构建的结论。

### 1.1.0

1.1.0 引入可见的本地 PetPack 导入流程、半写实退休猫 0.1.0、Todo
全局快捷入口和完整的本地内容确认。该候选保留自己的 `ALPHA-GO` 证据，不由
1.1.1 覆盖。

### 1.0.1

1.0.1 修正高 DPI 下的角色正文重采样和实时动作文字层。该候选也保留
自己的 `ALPHA-GO` 证据。

### 文档链与后续重点

本轮未发布的文档工作把当前事实、版本变化、未来路线和角色创作接成一条主链，
并把 2026-08-28 的审计与迁移路线降为历史快照。用户确认后续优先扩展角色库，
当前计划见 [未来工作](FUTURE_WORK.md)。这项文档变更不改变 1.1.1 的 EXE，也不
继承它的验收结论。

本轮内部源树回归共收集 869 项，856 passed、13 skipped；脱敏后的单 root commit
公开快照同样收集 869 项，855 passed、14 skipped。公开快照多出的 skip 是检查
“内部仍保留冻结 0.1.0 包”的测试：该旧包按公开策略精确排除，0.1.1 及产品能力
测试仍全部通过。Markdown 相对链接与孤立文档检查为 2 passed，`git diff --check`
通过。这些结果只验证当前源码与文档，不产生新的 frozen artifact。

## 2026-08-23 至 2026-08-31

这一阶段完成了 1.0.0 单角色桌宠基线，并逐步落地透明窗口、退休倒计时、动作
控制、托盘、自启、PetPack 安全验证、不可变角色库、两阶段切换、控制面板、Todo、
隐私治理和可信发布流水线。逐提交记录保留在 Git 历史中；当时的事实快照见
[2026-08-28 状态审计](CURRENT_STATE_AUDIT.md)，迁移顺序见
[v2 已执行路线图](IMPLEMENTATION_ROADMAP.md)。

## 新增记录时需要写清楚

每个发布条目至少包含版本、日期、源提交、用户可见变化、兼容或迁移边界、验证
结论和仍未完成的门禁。探索性美术、临时 benchmark 和未发布工作应明确标成
`EXPLORATORY` 或“未发布”，不能写进正式构建身份。

# RetirementPet 安全报告政策

RetirementPet 是仍在演进的 Windows Alpha 软件。安全问题请私下报告，不要在公开
Issue、Discussion、Pull Request、截图或日志中披露可利用细节。

## 支持范围

| 版本 | 安全支持 |
|---|---|
| GitHub 上最新的 Alpha prerelease | 接受安全报告并优先评估 |
| `main` 的最新源码 | 接受可在最新提交复现的报告 |
| 旧 release、个人修改版和第三方角色包 | 仅尽力协助，不保证修复 |

Alpha 的支持范围不等同于 Production 认证。当前环境门禁与已知限制见
[实施状态](docs/IMPLEMENTATION_STATUS.md)。

## 如何私下报告

优先使用 GitHub 的
[Private Vulnerability Reporting](https://github.com/Bpig-C/RetirementPet/security/advisories/new)。
如果该入口暂时不可用，请不要改为公开 Issue；等待仓库维护者启用私密报告入口。

报告尽量包含：

- 受影响版本、commit 或 Release 名称；
- Windows 版本、架构和必要的运行环境；
- 最小复现步骤、预期影响及是否需要用户交互；
- 已脱敏的日志、错误代码或概念验证；
- 你建议的披露时间和署名方式。

请删除用户名、绝对个人路径、机器名、令牌、证书、真实待办和音乐路径。不要上传
`tasks.db`、完整 AppData、完整 `.release` 证据目录、第三方私有角色包或用户文件。
如果最小复现需要恶意 `.petpack`，先在私密报告中描述其结构；维护者确认安全传输
方式前不要公开附件。

项目目前没有付费漏洞奖励计划，也不承诺固定响应时限。维护者会尽快确认报告、
评估影响，并在修复和用户更新准备好后协调披露。未经协调，请不要公开仍可利用的
细节。

## 适合安全渠道的问题

包括但不限于：

- PetPack 路径穿越、压缩炸弹、解析器绕过、任意文件写入或代码执行；
- 单实例 IPC、自启注册表、角色库事务或更新流程造成的权限边界突破；
- 日志、崩溃信息或 UI 意外泄露任务、路径或其他本地数据；
- 打包依赖、DLL 搜索路径或 frozen artifact 的可利用完整性问题；
- 绕过本地内容确认、信任通道或不可变 Revision 约束。

普通崩溃、显示错误、功能建议和没有安全影响的性能问题，请使用对应公开 Issue
模板。第三方内容的版权或商标问题可先私下提供权利证明和受影响路径，避免在公开
讨论中再次分发争议内容。

## 安全研究边界

只测试你拥有或获准测试的机器和数据。不要访问他人设备、公开私人信息、进行持续
拒绝服务，或借测试传播恶意角色包。请优先使用最小、无个人数据的本地夹具。
